resources:
  - istio-gateway.yaml
  - web.yaml

# Change the image name and version
images:
  - name: web
    newName: web
    newTag: v1.0

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
